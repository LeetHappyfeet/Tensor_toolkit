# Memory-aware execution

This document has been superseded by [MEMORY_AND_STORAGE.md](MEMORY_AND_STORAGE.md).

The earlier version described the first time-axis-only tiled executor and stated that final outputs still had to fit in RAM. The current Development branch now supports multidimensional block planning and disk-backed NumPy memmap output, so those statements are obsolete.

Existing links are retained here for compatibility.
